# Build After Edits Rule

After making any code changes to this addon, always:
1. Run the build script (`./build.sh`) to verify compilation
2. Commit changes to git with a descriptive message
3. Push to GitHub (`git push origin main`)

This ensures the GitHub repository at https://github.com/Todd0042/Accessibuilds stays in sync with local development.
